# Postgres database connection details
POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=6432
POSTGRES_DB=postgres

# Collection exercise loader config
COLLEX_LOAD_CONFIG=collex-config.json

# Collection exercise event loader config
COLLEX_EVENT_LOAD_CONFIG=event-config.json

# OFATS event loader config
OFATS_EVENT_LOAD_CONFIG=event-config.json
